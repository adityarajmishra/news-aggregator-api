declare namespace Express {
    interface Request {
      user: any; // Define the 'user' property with the appropriate type
      message: string; // Define the 'message' property with the appropriate type
    }
  }